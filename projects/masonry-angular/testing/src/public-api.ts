/*
 * Test doubles for applications that render a grid.
 *
 * Published as `masonry-angular/testing` so it never reaches a production
 * bundle.
 */
export { FakeAnimation, GridTestHarness } from './harness';
